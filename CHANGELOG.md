# 更新记录

## 0.1.13（Build 14）— 2026-09-18

- 新增折角便签与金色星星的应用图标。
- 状态栏使用折角便签与空心星星的单色线条图标，不显示名称。
- 修正菜单栏图标的接入方式：改用标准模板图片，避免自绘图形在菜单栏中不显示。
- 补充图片类型、模板标记、尺寸、无障碍名称及深浅色渲染测试。
- 基于 0.1.9 功能版本，不包含未发布的分隔线实验改动。
- 自动化验证：核心 29 项、UI 117 项通过；真实菜单栏显示仍待实机确认。

## 0.1.9 — 2026-09-15

- 修复恢复会话时主题、透明度及置顶被全局默认值覆盖的问题。
- 初始化窗口使用保存的便签偏好，仅后续主动修改全局设置时同步覆盖；新建便签仍使用默认设置。
- 补充真实窗口退出重开、实际 floating 层级、二次重开及全局同步回归。
- 本次发布包含此前未单独发布的 0.1.7、0.1.8 改动。

## 0.1.8 — 2026-09-14（本地候选，尚未发布）

- 正常退出自动保存会话，下次恢复已保存便签、未命名草稿及窗口位置，不再逐张弹文件保存提示。
- 主动关闭仍保留保存／不保存／取消，已关闭便签不再复活。
- 会话原子写入，写入失败阻止退出；兼容旧草稿，保留文件冲突基线和缺失文件内容。
- 损坏恢复记录提示并保留原始备份，重复文件的不同内容转为草稿。
- 包含 0.1.7 仅菜单栏模式，不占 Dock 或 Command-Tab。

## 0.1.7 — 2026-09-14（本地候选，尚未发布）

- 改为仅菜单栏模式，运行时不占 Dock 或 Command-Tab 位置。
- 保留顶部星星的新建、打开、设置、显示全部和退出入口。
- 同步启动包配置和运行激活策略，保留现有窗口、重开和保存确认逻辑。

## 0.1.6 — 2026-09-14

- 未完成待办框改为无填色，保持清晰边框。
- 已完成待办框随五种便签主题使用低饱和浅填色及深色勾号，不再固定薄荷绿。
- 待办框配色独立于背景透明度，切换主题即时更新；尺寸、点击、Markdown 和撤销行为不变。
- 增加五主题、三档背景透明度的实际像素及编辑状态回归检查。

## 0.1.5 — 2026-09-13

- 设置合并为一组，全局颜色、透明度、字号、置顶即时生效，新建与重开沿用。
- 透明度 0%–100%，取消隐藏映射及独立磨砂底板，正文和待办框不随背景淡化。
- 系统减少透明度开启时禁用滑块并提示，关闭后恢复保存值。
- 一次性迁移旧背景浓度，保留 Markdown 正文和窗口位置。
- 增加背景像素、原生滑块、全局同步、重开及迁移检查。

## 0.1.4 — 2026-09-10（本地候选，尚未发布）

- 改为普通 macOS 应用，提供 Dock 入口与应用主菜单；点击正文时激活应用。
- 点击 Dock 显示已有便签，不再每次新建。
- 设置区分当前便签、后续新建默认值和全局字号；当前便签的颜色、背景浓度、置顶即时应用并持久化。
- 新增设置目标保持、作用范围、持久化及 Dock 重开行为检查。

## 0.1.3 — 2026-09-09

- 修复空便签及短便签点击正文下方空白处后，无法直接输入的问题。
- 显式交接正文输入焦点，并在改名框收起后恢复被清除的焦点；不从其他输入控件抢焦点。
- 空白区域点击定位到文末，不插入额外空行；保留原生文字点击和选区行为。
- 新增实际鼠标点击及键盘输入检查，覆盖空文本、短文本、改名后及失焦后场景。

## 0.1.2 — 2026-09-08

- 将版本号递增为 0.1.2（Build 3），区分此前未递增编号的开发构建。
- 标题栏仅名字区域用于单击改名，其余空白区域交给原生窗口拖动。
- 保留正文点击确认改名并收起、编辑时仅移开鼠标不提交的交互。
- UI 检查使用完整 AppKit 事件循环，并补全鼠标释放事件。
- 补充安装使用、架构、贡献、安全与发布说明。

本版包含此前源码快照已有的保存冲突保护、恢复状态提示、主题对比度、实时字号和输入法/撤销同步修复。
这是首个计划通过 GitHub Release 分发的安装包，不把旧的开发快照当作已发布版本。
