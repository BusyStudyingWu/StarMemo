# 公开准备检查（2026-09-16）

本次为代码结构与文档检查，不是完整安全审计。未修改应用功能，未提交或发布本地候选版本。

## 结构结论

- App / UI / Core / Vendor 分工明确；Core 不依赖界面或编辑引擎，当前无需为公开仓库调整目录。
- 当前正文入口为 NoteWindowView → StarMemoMarkdownEditor → MarkdownEngine。旧编辑器仍在编译目标内，后续可在确认引用和测试用途后隔离，不能直接删除。
- NoteWindowCoordinator 约 572 行，同时承担窗口集合、保存冲突、重命名和会话恢复。后续可优先拆分会话与文件操作；本轮不做行为风险较高的重构。
- 构建脚本包含特定 SDK 的自动选择。贡献指南说明了 SDK 兼容与参数传递，但没有验证所有工具链组合。

## 文档修改

- 首页聚焦功能、安装、使用与开发入口，移除本地候选版本和测试流水账。
- 修正菜单栏模式、每张便签外观恢复及会话存储的架构说明。
- 合并使用指南中的重复描述；保留未签名、无应用层加密、备份及平台限制。
- 保留 MIT 及第三方来源说明，区分 AtomGit 标签与 GitHub Release 安装包。
- 当前两份旧计划文档中的个人绝对路径改为仓库根目录描述；未重写历史。

## 检查结果与边界

- 扫描本地全部 6 个可达提交的文本，未匹配常见私钥头、GitHub/GitLab/AWS 凭据模式及长字符串密码赋值模式。
- 历史文件名未发现所查的 .env、.netrc、私钥、证书包和 session.json；提交邮箱均为 GitHub noreply 地址。
- 两份历史计划中仍有个人绝对路径，修改当前文件不能清除历史。
- GitHub Actions 查询为 0 次运行；发布附件列表为 DMG 与校验文件，未对附件内部或所有远程 Issue/讨论做敏感内容审计。
- 五份主要文档共 14 个本地链接检查通过，git diff --check 通过。
- 本次仅改文档，未重新执行应用构建和 UI 测试；不复用旧测试数量作本轮运行结果。
- 本次文档提交不包含未发布的分隔线修改、版本号或候选版本更新记录。

基础模式扫描可能漏报；以上结果不能证明代码无漏洞或历史绝无敏感信息。
